# 🌊 Algorithme du Flux

Une application interactive pour analyser la cohérence entre contraintes
(planification, logistique, design) et générer des compromis via l’**Algorithme du Flux**.

## 🚀 Utilisation locale

1. Cloner le dépôt :
   ```bash
   git clone https://github.com/<ton-username>/flux-algorithme.git
   cd flux-algorithme
